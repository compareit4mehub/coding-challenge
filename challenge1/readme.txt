Specify which connection string to use with the "DatabaseName" app setting in web.config.
Connection strings are listed at the bottom.

Run the CreateDatabase.sql script to create a local database, then adjust the connection string accordingly.
Otherwise the Azure database is available as "challenge1Context".

Let nuget do its thing and then run the app!